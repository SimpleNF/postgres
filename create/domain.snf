# https://www.postgresql.org/docs/17/sql-createdomain.html

CREATE DOMAIN name [ AS ] type
    [ COLLATE collate ]
    [ DEFAULT value_expression ]
    constraint [...]

# WHERE constraint
[ CONSTRAINT name ] { NOT NULL | NULL | CHECK (check_expression) }

# RETURN check_expression
BOOLEAN