# https://www.postgresql.org/docs/17/sql-createdomain.html

CREATE DOMAIN name [ AS ] type
    [ COLLATE collate ]
    [ DEFAULT default ]
    [ constraint [...] ]

# WHERE constraint
[ CONSTRAINT name ] { NOT NULL | NULL | CHECK (check) }