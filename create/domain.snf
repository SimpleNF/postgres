# https://www.postgresql.org/docs/18/sql-createdomain.html

CREATE DOMAIN name [ AS ] type
    [ COLLATE collate ]
    [ DEFAULT value_expression ]
    constraint [...]

# WHERE constraint
[ CONSTRAINT constraint ] { NOT NULL | NULL | CHECK ( boolean_expression ) }
