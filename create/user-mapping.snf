# https://www.postgresql.org/docs/18/sql-createusermapping.html

CREATE USER MAPPING [ IF NOT EXISTS ] FOR role_public
    SERVER server
    [ OPTIONS ( { option 'value' } [, ...] ) ]

# WHERE role_public
{ role_name | CURRENT_ROLE | CURRENT_USER | PUBLIC }
