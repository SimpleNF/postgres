# https://www.postgresql.org/docs/17/sql-createstatistics.html

CREATE STATISTICS [ IF NOT EXISTS ] [ name ]
    ON ( on )
    FROM table

CREATE STATISTICS [ IF NOT EXISTS ] [ name ] [ ( kind [, ...] ) ]
    ON { colname | ( col_expression ) } [, ...]
    FROM table